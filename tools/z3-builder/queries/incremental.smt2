(set-logic QF_AUFLIA)
(set-option :random-seed 0)
(declare-const a (Array Int Int))
(declare-const i Int)
(assert (= (select (store a i 7) i) 7))
(check-sat)
(push 1)
(assert (= (select (store a i 7) i) 8))
(check-sat)
(pop 1)
(assert (= (select a i) 9))
(check-sat)
(exit)
